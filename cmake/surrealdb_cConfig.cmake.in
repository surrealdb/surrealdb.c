@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/surrealdb_cTargets.cmake")

# Provide an alias for the exported target
if(NOT TARGET surrealdb::surrealdb_c)
    add_library(surrealdb::surrealdb_c ALIAS surrealdb::surrealdb_c_export)
endif()

check_required_components(surrealdb_c)
